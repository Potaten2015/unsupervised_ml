function [ X ] = MakeDataSet( filename )

    Img = imread(filename);
    
    [R C] = find( Img == 0 );
    
    size(R)
    size(C)

    plot(R,C,'xk');
    
    X = [ R C ];
    si

end